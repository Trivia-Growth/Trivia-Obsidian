Selo / eyebrow da Sinérgica — rótulo de seção em caixa-alta espaçada ou tag de status.

```jsx
<Badge variant="orange">Manutenção Predial</Badge>
<Badge variant="success" uppercase={false}>Concluído</Badge>
```

Variantes: `orange` (gradiente), `navy`, `success`, `neutral`, `outline`. Tamanhos `sm`/`md`.
