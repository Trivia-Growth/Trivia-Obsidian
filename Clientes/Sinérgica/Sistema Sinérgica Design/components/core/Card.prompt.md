Cartão de superfície da Sinérgica — branco, cantos generosos e sombra fria.

```jsx
<Card><h3>Manutenção preventiva</h3><p>...</p></Card>
<Card variant="dark" pad="lg">...</Card>
```

Variantes: `light` (padrão), `dark` (navy), `gradient`. Padding `sm`/`md`/`lg`.
