.DS_Store
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# Arquivos de mídia (ficam apenas localmente)
*.webm
*.mkv
*.mp4
*.mov
*.avi
*.zip

# Notas com segredos — apenas locais, nunca sincronizam
*.secret.md

# Backups de dados grandes (listas de contatos, exports) — ficam só locais
**/Flowbiz_backup-*/
